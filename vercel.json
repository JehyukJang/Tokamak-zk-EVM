{
  "buildCommand": "cd packages/playground && npm run build",
  "outputDirectory": "packages/playground/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}