// The producer-owned normalized library contract is intentionally free of
// arithmetic dependencies so runtime consumers and build scripts use the
// same admission code.
include!("../../../build-support/normalized_library.rs");
